/* global describe, it */

describe( 'CurvePath', () => {

    it( 'is bundlable', () => {

       should.exist( Three['CurvePath'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['CurvePath']() )

    } )

} )
