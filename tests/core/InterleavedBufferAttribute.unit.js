/* global describe, it */

describe( 'InterleavedBufferAttribute', () => {

    it( 'is bundlable', () => {

       should.exist( Three['InterleavedBufferAttribute'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['InterleavedBufferAttribute']() )

    } )

} )
