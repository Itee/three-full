/* global describe, it */

describe( 'ShapePath', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ShapePath'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ShapePath']() )

    } )

} )
