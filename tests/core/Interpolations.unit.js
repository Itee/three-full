/* global describe, it */

describe( 'Interpolations', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Interpolations'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Interpolations']() )

    } )

} )
