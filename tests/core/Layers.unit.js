/* global describe, it */

describe( 'Layers', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Layers'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Layers']() )

    } )

} )
