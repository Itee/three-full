/* global describe, it */

describe( 'VolumeSlice', () => {

    it( 'is bundlable', () => {

       should.exist( Three['VolumeSlice'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['VolumeSlice']() )

    } )

} )
