/* global describe, it */

describe( 'VerticalTiltShiftShader', () => {

    it( 'is bundlable', () => {

       should.exist( Three['VerticalTiltShiftShader'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['VerticalTiltShiftShader']() )

    } )

} )
