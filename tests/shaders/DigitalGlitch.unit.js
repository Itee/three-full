/* global describe, it */

describe( 'DigitalGlitch', () => {

    it( 'is bundlable', () => {

       should.exist( Three['DigitalGlitch'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['DigitalGlitch']() )

    } )

} )
