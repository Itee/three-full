/* global describe, it */

describe( 'HorizontalTiltShiftShader', () => {

    it( 'is bundlable', () => {

       should.exist( Three['HorizontalTiltShiftShader'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['HorizontalTiltShiftShader']() )

    } )

} )
