/* global describe, it */

describe( 'ShaderGodRays', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ShaderGodRays'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ShaderGodRays']() )

    } )

} )
