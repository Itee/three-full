/* global describe, it */

describe( 'ConvexObjectBreaker', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ConvexObjectBreaker'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ConvexObjectBreaker']() )

    } )

} )
