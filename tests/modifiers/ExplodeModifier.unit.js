/* global describe, it */

describe( 'ExplodeModifier', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ExplodeModifier'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ExplodeModifier']() )

    } )

} )
