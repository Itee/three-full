/* global describe, it */

describe( 'SkeletonUtils', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SkeletonUtils'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SkeletonUtils']() )

    } )

} )
