/* global describe, it */

describe( 'SceneUtils', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SceneUtils'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SceneUtils']() )

    } )

} )
