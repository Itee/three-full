/* global describe, it */

describe( 'GeometryUtils', () => {

    it( 'is bundlable', () => {

       should.exist( Three['GeometryUtils'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['GeometryUtils']() )

    } )

} )
