/* global describe, it */

describe( 'OutlineEffect', () => {

    it( 'is bundlable', () => {

       should.exist( Three['OutlineEffect'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['OutlineEffect']() )

    } )

} )
