/* global describe, it */

describe( 'ParallaxBarrierEffect', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ParallaxBarrierEffect'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ParallaxBarrierEffect']() )

    } )

} )
