/* global describe, it */

describe( 'BloomPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['BloomPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['BloomPass']() )

    } )

} )
