/* global describe, it */

describe( 'BokehPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['BokehPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['BokehPass']() )

    } )

} )
