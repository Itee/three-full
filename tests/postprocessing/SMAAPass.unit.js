/* global describe, it */

describe( 'SMAAPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SMAAPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SMAAPass']() )

    } )

} )
