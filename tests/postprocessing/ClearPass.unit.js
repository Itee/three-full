/* global describe, it */

describe( 'ClearPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ClearPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ClearPass']() )

    } )

} )
