/* global describe, it */

describe( 'GlitchPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['GlitchPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['GlitchPass']() )

    } )

} )
