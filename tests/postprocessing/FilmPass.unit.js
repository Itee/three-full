/* global describe, it */

describe( 'FilmPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['FilmPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['FilmPass']() )

    } )

} )
