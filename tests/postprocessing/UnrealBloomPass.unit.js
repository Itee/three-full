/* global describe, it */

describe( 'UnrealBloomPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['UnrealBloomPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['UnrealBloomPass']() )

    } )

} )
