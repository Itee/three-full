/* global describe, it */

describe( 'SSAOPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SSAOPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SSAOPass']() )

    } )

} )
