/* global describe, it */

describe( 'SAOPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SAOPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SAOPass']() )

    } )

} )
