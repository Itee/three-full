/* global describe, it */

describe( 'AfterimagePass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['AfterimagePass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['AfterimagePass']() )

    } )

} )
