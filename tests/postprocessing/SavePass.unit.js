/* global describe, it */

describe( 'SavePass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SavePass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SavePass']() )

    } )

} )
