/* global describe, it */

describe( 'DotScreenPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['DotScreenPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['DotScreenPass']() )

    } )

} )
