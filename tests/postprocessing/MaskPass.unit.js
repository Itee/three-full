/* global describe, it */

describe( 'MaskPass', () => {

    it( 'is bundlable', () => {

       should.exist( Three['MaskPass'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['MaskPass']() )

    } )

} )
