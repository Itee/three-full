/* global describe, it */

describe( 'Ray', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Ray'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Ray']() )

    } )

} )
