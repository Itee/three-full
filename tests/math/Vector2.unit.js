/* global describe, it */

describe( 'Vector2', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Vector2'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Vector2']() )

    } )

} )
