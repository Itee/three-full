/* global describe, it */

describe( 'Box3', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Box3'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Box3']() )

    } )

} )
