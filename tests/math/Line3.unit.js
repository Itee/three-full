/* global describe, it */

describe( 'Line3', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Line3'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Line3']() )

    } )

} )
