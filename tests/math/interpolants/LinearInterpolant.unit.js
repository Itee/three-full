/* global describe, it */

describe( 'LinearInterpolant', () => {

    it( 'is bundlable', () => {

       should.exist( Three['LinearInterpolant'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['LinearInterpolant']() )

    } )

} )
