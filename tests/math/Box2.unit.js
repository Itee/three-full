/* global describe, it */

describe( 'Box2', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Box2'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Box2']() )

    } )

} )
