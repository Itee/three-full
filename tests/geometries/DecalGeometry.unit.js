/* global describe, it */

describe( 'DecalGeometry', () => {

    it( 'is bundlable', () => {

       should.exist( Three['DecalGeometry'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['DecalGeometry']() )

    } )

} )
