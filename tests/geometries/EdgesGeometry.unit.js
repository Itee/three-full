/* global describe, it */

describe( 'EdgesGeometry', () => {

    it( 'is bundlable', () => {

       should.exist( Three['EdgesGeometry'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['EdgesGeometry']() )

    } )

} )
