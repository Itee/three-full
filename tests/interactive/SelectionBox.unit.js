/* global describe, it */

describe( 'SelectionBox', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SelectionBox'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SelectionBox']() )

    } )

} )
