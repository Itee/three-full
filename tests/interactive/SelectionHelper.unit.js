/* global describe, it */

describe( 'SelectionHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SelectionHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SelectionHelper']() )

    } )

} )
