/* global describe, it */

describe( 'ArrowHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ArrowHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ArrowHelper']() )

    } )

} )
