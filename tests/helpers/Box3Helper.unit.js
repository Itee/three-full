/* global describe, it */

describe( 'Box3Helper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Box3Helper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Box3Helper']() )

    } )

} )
