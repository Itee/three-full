/* global describe, it */

describe( 'SpotLightHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SpotLightHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SpotLightHelper']() )

    } )

} )
