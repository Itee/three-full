/* global describe, it */

describe( 'FaceNormalsHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['FaceNormalsHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['FaceNormalsHelper']() )

    } )

} )
