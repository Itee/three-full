/* global describe, it */

describe( 'SkeletonHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SkeletonHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SkeletonHelper']() )

    } )

} )
