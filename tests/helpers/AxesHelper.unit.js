/* global describe, it */

describe( 'AxesHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['AxesHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['AxesHelper']() )

    } )

} )
