/* global describe, it */

describe( 'PolarGridHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['PolarGridHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['PolarGridHelper']() )

    } )

} )
