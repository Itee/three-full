/* global describe, it */

describe( 'VertexNormalsHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['VertexNormalsHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['VertexNormalsHelper']() )

    } )

} )
