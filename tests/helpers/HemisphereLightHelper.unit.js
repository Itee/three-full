/* global describe, it */

describe( 'HemisphereLightHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['HemisphereLightHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['HemisphereLightHelper']() )

    } )

} )
