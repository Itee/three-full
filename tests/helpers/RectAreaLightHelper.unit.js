/* global describe, it */

describe( 'RectAreaLightHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['RectAreaLightHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['RectAreaLightHelper']() )

    } )

} )
