/* global describe, it */

describe( 'PlaneHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['PlaneHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['PlaneHelper']() )

    } )

} )
