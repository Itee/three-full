/* global describe, it */

describe( 'LineSegments2', () => {

    it( 'is bundlable', () => {

       should.exist( Three['LineSegments2'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['LineSegments2']() )

    } )

} )
