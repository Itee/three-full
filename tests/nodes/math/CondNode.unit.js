/* global describe, it */

describe( 'CondNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['CondNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['CondNode']() )

    } )

} )
