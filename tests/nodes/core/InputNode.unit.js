/* global describe, it */

describe( 'InputNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['InputNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['InputNode']() )

    } )

} )
