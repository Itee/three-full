/* global describe, it */

describe( 'ResolutionNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ResolutionNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ResolutionNode']() )

    } )

} )
