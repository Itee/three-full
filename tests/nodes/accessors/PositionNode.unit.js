/* global describe, it */

describe( 'PositionNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['PositionNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['PositionNode']() )

    } )

} )
