/* global describe, it */

describe( 'ColorsNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ColorsNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ColorsNode']() )

    } )

} )
