/* global describe, it */

describe( 'BumpMapNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['BumpMapNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['BumpMapNode']() )

    } )

} )
