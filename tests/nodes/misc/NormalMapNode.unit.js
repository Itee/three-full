/* global describe, it */

describe( 'NormalMapNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['NormalMapNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['NormalMapNode']() )

    } )

} )
