/* global describe, it */

describe( 'FloatNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['FloatNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['FloatNode']() )

    } )

} )
