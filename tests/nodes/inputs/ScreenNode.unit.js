/* global describe, it */

describe( 'ScreenNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ScreenNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ScreenNode']() )

    } )

} )
