/* global describe, it */

describe( 'BoolNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['BoolNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['BoolNode']() )

    } )

} )
