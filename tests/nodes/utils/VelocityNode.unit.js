/* global describe, it */

describe( 'VelocityNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['VelocityNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['VelocityNode']() )

    } )

} )
