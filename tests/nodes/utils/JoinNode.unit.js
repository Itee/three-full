/* global describe, it */

describe( 'JoinNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['JoinNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['JoinNode']() )

    } )

} )
