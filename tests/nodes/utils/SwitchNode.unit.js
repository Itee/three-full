/* global describe, it */

describe( 'SwitchNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SwitchNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SwitchNode']() )

    } )

} )
