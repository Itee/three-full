/* global describe, it */

describe( 'MaxMIPLevelNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['MaxMIPLevelNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['MaxMIPLevelNode']() )

    } )

} )
