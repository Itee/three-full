/* global describe, it */

describe( 'BlurNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['BlurNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['BlurNode']() )

    } )

} )
