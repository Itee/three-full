/* global describe, it */

describe( 'BlinnShininessExponentNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['BlinnShininessExponentNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['BlinnShininessExponentNode']() )

    } )

} )
