/* global describe, it */

describe( 'RoughnessToBlinnExponentNode', () => {

    it( 'is bundlable', () => {

       should.exist( Three['RoughnessToBlinnExponentNode'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['RoughnessToBlinnExponentNode']() )

    } )

} )
