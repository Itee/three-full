/* global describe, it */

describe( 'CCDIKSolver', () => {

    it( 'is bundlable', () => {

       should.exist( Three['CCDIKSolver'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['CCDIKSolver']() )

    } )

} )
