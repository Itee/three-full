/* global describe, it */

describe( 'MMDAnimationHelper', () => {

    it( 'is bundlable', () => {

       should.exist( Three['MMDAnimationHelper'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['MMDAnimationHelper']() )

    } )

} )
