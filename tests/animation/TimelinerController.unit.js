/* global describe, it */

describe( 'TimelinerController', () => {

    it( 'is bundlable', () => {

       should.exist( Three['TimelinerController'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['TimelinerController']() )

    } )

} )
