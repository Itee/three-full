/* global describe, it */

describe( 'VectorKeyframeTrack', () => {

    it( 'is bundlable', () => {

       should.exist( Three['VectorKeyframeTrack'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['VectorKeyframeTrack']() )

    } )

} )
