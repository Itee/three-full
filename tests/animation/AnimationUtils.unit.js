/* global describe, it */

describe( 'AnimationUtils', () => {

    it( 'is bundlable', () => {

       should.exist( Three['AnimationUtils'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['AnimationUtils']() )

    } )

} )
