/* global describe, it */

describe( 'PaintViveController', () => {

    it( 'is bundlable', () => {

       should.exist( Three['PaintViveController'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['PaintViveController']() )

    } )

} )
