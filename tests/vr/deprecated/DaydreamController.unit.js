/* global describe, it */

describe( 'DaydreamController', () => {

    it( 'is bundlable', () => {

       should.exist( Three['DaydreamController'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['DaydreamController']() )

    } )

} )
