/* global describe, it */

describe( 'GearVRController', () => {

    it( 'is bundlable', () => {

       should.exist( Three['GearVRController'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['GearVRController']() )

    } )

} )
