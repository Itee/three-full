/* global describe, it */

describe( 'UniformsUtils', () => {

    it( 'is bundlable', () => {

       should.exist( Three['UniformsUtils'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['UniformsUtils']() )

    } )

} )
