/* global describe, it */

describe( 'WebGLMorphtargets', () => {

    it( 'is bundlable', () => {

       should.exist( Three['WebGLMorphtargets'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['WebGLMorphtargets']() )

    } )

} )
