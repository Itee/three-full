/* global describe, it */

describe( 'WebGLRenderStates', () => {

    it( 'is bundlable', () => {

       should.exist( Three['WebGLRenderStates'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['WebGLRenderStates']() )

    } )

} )
