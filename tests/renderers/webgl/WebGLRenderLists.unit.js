/* global describe, it */

describe( 'WebGLRenderLists', () => {

    it( 'is bundlable', () => {

       should.exist( Three['WebGLRenderLists'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['WebGLRenderLists']() )

    } )

} )
