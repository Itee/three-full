/* global describe, it */

describe( 'WebGLClipping', () => {

    it( 'is bundlable', () => {

       should.exist( Three['WebGLClipping'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['WebGLClipping']() )

    } )

} )
