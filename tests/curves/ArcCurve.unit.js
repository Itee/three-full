/* global describe, it */

describe( 'ArcCurve', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ArcCurve'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ArcCurve']() )

    } )

} )
