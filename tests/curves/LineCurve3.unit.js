/* global describe, it */

describe( 'LineCurve3', () => {

    it( 'is bundlable', () => {

       should.exist( Three['LineCurve3'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['LineCurve3']() )

    } )

} )
