/* global describe, it */

describe( 'LineCurve', () => {

    it( 'is bundlable', () => {

       should.exist( Three['LineCurve'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['LineCurve']() )

    } )

} )
