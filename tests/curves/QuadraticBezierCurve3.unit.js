/* global describe, it */

describe( 'QuadraticBezierCurve3', () => {

    it( 'is bundlable', () => {

       should.exist( Three['QuadraticBezierCurve3'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['QuadraticBezierCurve3']() )

    } )

} )
