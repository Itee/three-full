/* global describe, it */

describe( 'CurveExtras', () => {

    it( 'is bundlable', () => {

       should.exist( Three['CurveExtras'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['CurveExtras']() )

    } )

} )
