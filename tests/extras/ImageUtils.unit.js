/* global describe, it */

describe( 'ImageUtils', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ImageUtils'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ImageUtils']() )

    } )

} )
