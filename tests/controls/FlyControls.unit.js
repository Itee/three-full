/* global describe, it */

describe( 'FlyControls', () => {

    it( 'is bundlable', () => {

       should.exist( Three['FlyControls'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['FlyControls']() )

    } )

} )
