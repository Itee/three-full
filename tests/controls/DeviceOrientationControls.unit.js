/* global describe, it */

describe( 'DeviceOrientationControls', () => {

    it( 'is bundlable', () => {

       should.exist( Three['DeviceOrientationControls'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['DeviceOrientationControls']() )

    } )

} )
