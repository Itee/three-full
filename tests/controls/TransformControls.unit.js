/* global describe, it */

describe( 'TransformControls', () => {

    it( 'is bundlable', () => {

       should.exist( Three['TransformControls'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['TransformControls']() )

    } )

} )
