/* global describe, it */

describe( 'MapControls', () => {

    it( 'is bundlable', () => {

       should.exist( Three['MapControls'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['MapControls']() )

    } )

} )
