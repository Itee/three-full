/* global describe, it */

describe( 'PointerLockControls', () => {

    it( 'is bundlable', () => {

       should.exist( Three['PointerLockControls'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['PointerLockControls']() )

    } )

} )
