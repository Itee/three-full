/* global describe, it */

describe( 'RectAreaLight', () => {

    it( 'is bundlable', () => {

       should.exist( Three['RectAreaLight'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['RectAreaLight']() )

    } )

} )
