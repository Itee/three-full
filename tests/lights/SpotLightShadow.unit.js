/* global describe, it */

describe( 'SpotLightShadow', () => {

    it( 'is bundlable', () => {

       should.exist( Three['SpotLightShadow'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['SpotLightShadow']() )

    } )

} )
