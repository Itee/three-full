/* global describe, it */

describe( 'Skeleton', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Skeleton'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Skeleton']() )

    } )

} )
