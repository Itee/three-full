/* global describe, it */

describe( 'MD2Character', () => {

    it( 'is bundlable', () => {

       should.exist( Three['MD2Character'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['MD2Character']() )

    } )

} )
