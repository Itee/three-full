/* global describe, it */

describe( 'Lensflare', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Lensflare'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Lensflare']() )

    } )

} )
