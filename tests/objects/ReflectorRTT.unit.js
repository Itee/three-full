/* global describe, it */

describe( 'ReflectorRTT', () => {

    it( 'is bundlable', () => {

       should.exist( Three['ReflectorRTT'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['ReflectorRTT']() )

    } )

} )
