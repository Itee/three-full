/* global describe, it */

describe( 'LineSegments', () => {

    it( 'is bundlable', () => {

       should.exist( Three['LineSegments'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['LineSegments']() )

    } )

} )
