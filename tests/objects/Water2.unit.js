/* global describe, it */

describe( 'Water2', () => {

    it( 'is bundlable', () => {

       should.exist( Three['Water2'] )

    } )

    it( 'is instanciable', () => {

       should.exist( new Three['Water2']() )

    } )

} )
